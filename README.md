# lettuce
os that somehow boots; kernel is getting written by rust, and everything else would be in lua
<br>(lua would be mostly used for GUI, but I still don't know how to implement it correctly)

run tests with `cargo test -p local-tests --tests`<br>
...or run ignored OS/QEMU checks with `cargo test -p local-tests --tests -- --ignored`
