[workspace]
members = [
  "kernel",
  "tools/local-tests",
]
resolver = "2"

[workspace.package]
edition = "2021"
version = "0.1.0"
authors = ["Alex"]
license = "GPL-3.0-only"

[workspace.lints.rust]
unsafe_op_in_unsafe_fn = "deny"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
